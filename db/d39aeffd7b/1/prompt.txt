What would it take to render the PixelData of a DICOM ? Just a question, don't write code

---

dcr only opens one dicom file (one slice), or diffs two dicoms. Let's add a Preview (enabled 'p' key) that displays a preview of the image. it uses the default windowing provided by the DICOM file, or a decent default. Let's use Kitty's image protocol. a fallback message is shown if the terminal isn't kitty.

---

[Request interrupted by user for tool use]